/**
 * Tmux Session Manager Adapter Barrel Export
 *
 * Provides session management via tmux terminal multiplexer.
 * Handles tmux session creation, attachment, listing, and termination.
 * Uses TmuxControlClient for communication with tmux.
 */

export { TmuxSessionManagerAdapter } from './tmux-session-manager-adapter'
